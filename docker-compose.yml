services:
  db:
    image: postgres:16
    volumes:
      - ./data/db:/var/lib/postgresql/data
    env_file:
      - .env
  web:
    build: ./mm_project
    command: ["gunicorn", "--bind", "0.0.0.0:80", "--worker-class", "gevent", "mm_project.wsgi:application"]
    volumes:
      - ./mm_project:/app
    ports:
      - "${DJANGO_PORT}:80"
    env_file:
      - .env
    depends_on:
      - db
